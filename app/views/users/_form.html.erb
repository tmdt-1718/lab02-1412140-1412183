<div class="container">
  <div class="row">
    <div class="col col-lg-8 offset-lg-4">
        <%= form_with(model: user, local: true,:defaults => { :input_html => { :class => "form-control" } }) do |form| %>
        <% if user.errors.any? %>
          <div id="error_explanation">
            <div class="well">
                <h2 class="well"><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
            </div>
      
            <ul>
            <% user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
        <div class="form-group">
          <%= form.text_field :email, :class => 'form-control' %>
        </div>
        <%= form.submit :Submit, class: 'btn btn-success' %>
      <% end %>
    </div>
  </div>
</div>